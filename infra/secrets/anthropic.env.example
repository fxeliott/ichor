# Anthropic API workspace `ichor-prod`
#
# ⚠️ NOT USED IN PRODUCTION (Voie D — see docs/decisions/ADR-009).
# This template is kept as historical documentation only. The production path
# uses Claude Max 20x via the apps/claude-runner local Win11 subprocess
# (no API key, no usage-based costs).
#
# Only fill if Eliot ever switches to Voie C (ADR-009 superseded).

ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_WITH_REAL_KEY
ANTHROPIC_WORKSPACE=ichor-prod
