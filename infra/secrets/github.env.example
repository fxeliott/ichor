# GitHub Personal Access Token for Eliot (fxeliott)
# Used by: gh CLI, GitHub Actions runner registration, Dependabot reviews
# Scopes needed: repo, workflow, write:packages (if publishing), read:org
# https://github.com/settings/tokens?type=beta

GITHUB_PAT=REPLACE  # github_pat_...
GITHUB_USER=fxeliott
